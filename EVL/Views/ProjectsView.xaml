<UserControl x:Class="EVL.Views.ProjectsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EVL.Views"
        mc:Ignorable="d"
        MaxHeight="375" MinHeight="300" MinWidth="600" FontSize="14">
    <Grid>
        <ToolBar Height="30" VerticalAlignment="Top">
            <ToggleButton x:Name="AddProjectBtn">Добавить</ToggleButton>
            <ToggleButton x:Name="DeleteProjectBtn">Удалить</ToggleButton>
        </ToolBar>
        <DataGrid x:Name="ProjectsTable" Margin="0,30,0,0" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№ Проекта" />
                <DataGridTextColumn Header="Название" />
                <DataGridTextColumn Header="Дата проекта" />
                <DataGridTextColumn Header="Описание" />
            </DataGrid.Columns>
            <DataGrid.Items>
                
            </DataGrid.Items>
        </DataGrid>
    </Grid>
</UserControl>
