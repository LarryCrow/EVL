<UserControl x:Class="EVL.Views.NewDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EVL.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DockPanel>
            <UniformGrid DockPanel.Dock="Bottom" Columns="3" Background="LightGray">
                <UniformGrid.Resources>
                    <Style TargetType="Button">
                        <Setter Property="MinWidth" Value="120"></Setter>
                        <Setter Property="Margin" Value="10 5"></Setter>
                    </Style>
                </UniformGrid.Resources>

                <Button>Добавить в базу</Button>
                <Button>Рассчитать</Button>
                <Button>Начать эксперименты</Button>
            </UniformGrid>
            <Grid Margin="0 2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition MinWidth="300"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <DockPanel>
                    <Grid DockPanel.Dock="Top" Margin="0 2">
                        <Grid.Resources>
                            <Style TargetType="Label">
                                <Setter Property="HorizontalAlignment" Value="Right"></Setter>
                            </Style>
                        </Grid.Resources>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>

                        <Label>Наименование</Label>
                        <TextBox Grid.Column="1"></TextBox>
                        <Label Grid.Row="1">Дата</Label>
                        <DatePicker Grid.Row="1" Grid.Column="1"></DatePicker>
                    </Grid>
                    <Grid Margin="0 2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="3*"/>
                        </Grid.ColumnDefinitions>
                        <DataGrid x:Name="AQuestionsTable" Margin="0 0 2 0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Свойство" Binding="{Binding Question.Name}"/>
                            </DataGrid.Columns>
                            <DataGrid.Items>
                            </DataGrid.Items>
                        </DataGrid>
                        <DataGrid x:Name="AAnswersTable" Grid.Column="1" Margin="2 0 0 0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Ответ" Binding="{Binding Answer.Name}"/>
                                <DataGridTextColumn Header="avalue" Binding="{Binding Value}"/>
                            </DataGrid.Columns>
                            <DataGrid.Items>
                            </DataGrid.Items>
                        </DataGrid>
                    </Grid>
                </DockPanel>
            </Grid>
        </DockPanel>
    </Grid>
</UserControl>
