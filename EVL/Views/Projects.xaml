<Window x:Class="EVL.Views.Projects"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EVL.Views"
        mc:Ignorable="d"
        Title="Projects" Height="300" Width="500">
    <Grid>
        <ToolBar Height="30" VerticalAlignment="Top">
            <ToggleButton>Добавить</ToggleButton>
            <ToggleButton>Удалить</ToggleButton>
            <ToggleButton>Сохранить</ToggleButton>
        </ToolBar>
        <DataGrid x:Name="phonesGrid" AutoGenerateColumns="True" Margin="0 30 0 0">
            <DataGrid.Items>
            </DataGrid.Items>
        </DataGrid>
    </Grid>
</Window>
